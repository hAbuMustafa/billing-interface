export async function POST() {
  // todo: receive patient name in a request
  // todo: perform database fuzzy search using extend-arabic-query on `People` table
  // todo: return id, name, and national_id for every patient
}
